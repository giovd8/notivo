import { FocusHandler } from './focus-handler';

describe('FocusHandler', () => {
  it('should create an instance', () => {
    const directive = new FocusHandler();
    expect(directive).toBeTruthy();
  });
});
